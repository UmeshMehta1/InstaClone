import React from "react";
import { Outlet } from "react-router-dom";

const EditProfile = () => {
  return (
    <div>
      edit prfile
      <Outlet />
    </div>
  );
};

export default EditProfile;
